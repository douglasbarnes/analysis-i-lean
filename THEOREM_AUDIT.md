# Analysis I theorem audit

Every `lemma`, `theorem`, `proposition`, and `corollary` environment in the supplied TeX is assigned one row. “Mathlib” means the result is intentionally skipped as an existing standard-library theorem, in accordance with the project brief. “Mathlib, corrected” means the mathematical result exists but the printed statement is malformed, underspecified, or expressed using a nonstandard local abstraction.

| ID | Source lines | Statement | Status | Mathlib target |
|---:|:---:|---|---|---|
| 1 | 84–86 | Let $\F$ be an ordered field and $x\in \F$. Then $x^2 \geq 0$. | Mathlib | `sq_nonneg` in `Mathlib.Algebra.Order.Ring.Defs` |
| 2 | 124–126 | Let $\F$ be an ordered field with the least upper bound property. Then the set $\{1, 2, 3, \cdots\}$ is not bounded above. | Mathlib | `exists_nat_gt` in `Mathlib.Algebra.Order.Archimedean.Basic` |
| 3 | 155–157 | $1/n \to 0$. | Mathlib | `tendsto_one_div_atTop_nhds_zero_nat` in `Mathlib.Analysis.SpecificLimits.Basic` |
| 4 | 175–177 | Every eventually bounded sequence is bounded. | Local proof | `AnalysisI.Local.Sequences.eventuallyBounded_bounded` |
| 5 | 188–190 | If $a_n \to a$ and $b_n \to b$, then $a_n + b_n \to a + b$. | Mathlib | `Filter.Tendsto.add` in `Mathlib.Topology.Algebra.Order` |
| 6 | 204–206 | Let $a_n \to a$ and $\lambda \in \R$. Then $\lambda a_n \to \lambda a$. | Mathlib | `Filter.Tendsto.const_mul / Filter.Tendsto.mul_const` in `Mathlib.Topology.Algebra.Order` |
| 7 | 214–216 | Let $(a_n)$ be bounded and $b_n \to 0$. Then $a_nb_n \to 0$. | Mathlib, corrected | `bounded-times-tendsto-zero lemma family` in `Mathlib.Topology.Algebra.Order` |
| 8 | 222–224 | Every convergent sequence is bounded. | Mathlib, corrected | `range of a convergent sequence is bounded` in `Mathlib.Topology.MetricSpace.Bounded` |
| 9 | 230–232 | Let $a_n\to a$ and $b_n\to b$. Then $a_nb_n\to ab$. | Mathlib | `Filter.Tendsto.mul` in `Mathlib.Topology.Algebra.Order` |
| 10 | 246–248 | Let $(a_n)$ be a sequence such that $(\forall n)\, a_n \not= 0$. Suppose that $a_n \to a$ and $a\not = 0$. Then $1/a_n \to 1/a$. | Mathlib | `Filter.Tendsto.inv₀` in `Mathlib.Topology.Algebra.Order` |
| 11 | 260–262 | If $a_n \to a, b_n \to b$, $b_n, b\not= 0$, then $a_n/b_n = a/b$. | Mathlib, corrected | `Filter.Tendsto.div` in `Mathlib.Topology.Algebra.Order` |
| 12 | 268–270 | Let $(a_n)$ and $(b_n)$ be sequences that both converge to a limit $x$. Suppose that $a_n \leq c_n \leq b_n$ for every $n$. Then $c_n \to x$. | Mathlib | `tendsto_of_tendsto_of_tendsto_of_le_of_le'` in `Mathlib.Order.Filter.Basic` |
| 13 | 329–331 | Least upper bound property $\Rightarrow$ monotone-sequences property. | Mathlib | `Monotone.tendsto_atTop_ciSup / conditionally complete order convergence` in `Mathlib.Order.Filter.Basic` |
| 14 | 340–342 | Let $(a_n)$ be a sequence and suppose that $a_n \to a$. If $(\forall n)\, a_n \leq x$, then $a\leq x$. | Mathlib | `le_of_tendsto` in `Mathlib.Topology.Order.Basic` |
| 15 | 349–351 | Monotone-sequences property $\Rightarrow$ Archimedean property. | Mathlib, corrected | `Archimedean typeclass consequences` in `Mathlib.Algebra.Order.Archimedean.Basic` |
| 16 | 361–363 | Monotone-sequences property $\Rightarrow$ least upper bound property. | Mathlib, corrected | `ConditionallyCompleteLinearOrder / monotone convergence` in `Mathlib.Order.ConditionallyCompleteLattice.Basic` |
| 17 | 380–382 | A sequence can have at most 1 limit. | Mathlib | `tendsto_nhds_unique` in `Mathlib.Topology.Sequences` |
| 18 | 388–390 | Let $\F$ be an ordered field with the monotone sequences property. Let $I_1\supseteq I_2 \supseteq \cdots$ be closed bounded non-empty intervals. Then $\bigcap_{n = 1}^\infty I_n \not= \emptyset$. | Mathlib | `nested compact interval / Cantor intersection theorem family` in `Mathlib.Topology.Order.MonotoneConvergence` |
| 19 | 399–401 | $\R$ is uncountable. | Mathlib | `Real.not_countable` in `Mathlib.SetTheory.Cardinal.Continuum` |
| 20 | 417–421 | Let $\F$ be an ordered field with the monotone sequences property (i.e.\ $\F = \mathbb{R}$). Then every bounded sequence has a convergent subsequence. | Mathlib | `isCompact_Icc.isSeqCompact` in `Mathlib.Topology.Instances.Real.Lemmas` |
| 21 | 444–446 | Every convergent sequence is Cauchy. | Mathlib | `Filter.Tendsto.cauchySeq` in `Mathlib.Topology.UniformSpace.Cauchy` |
| 22 | 452–454 | Let $(a_n)$ be a Cauchy sequence with a subsequence $(a_{n_k})$ that converges to $a$. Then $a_n\to a$. | Mathlib, corrected | `CauchySeq.tendsto_of_subseq_tendsto` in `Mathlib.Topology.UniformSpace.Cauchy` |
| 23 | 466–468 | Let $\F$ be an ordered field with the monotone-sequence property. Then every Cauchy sequence of $\F$ converges. | Mathlib | `cauchySeq_tendsto_of_complete` in `Mathlib.Topology.UniformSpace.CompleteSeparated` |
| 24 | 484–486 | Let $\F$ be an ordered field with the Archimedean property such that every Cauchy sequence converges. The $\F$ satisfies the monotone-sequences property. | Mathlib, corrected | `Archimedean + complete ⇒ monotone convergence` in `Mathlib.Order.ConditionallyCompleteLattice.Basic` |
| 25 | 550–556 | Let $(a_n)$ be a sequence. The following two statements are equivalent: \item $a_n\to a$ \item $\limsup a_n = \liminf a_n = a$. | Mathlib, corrected | `limsup/liminf convergence characterisations` in `Mathlib.Order.LiminfLimsup` |
| 26 | 597–599 | If $\displaystyle\sum_{n = 1}^\infty a_n$ converges. Then $a_n \to 0$. | Mathlib | `Summable.tendsto_atTop_zero` in `Mathlib.Topology.Algebra.InfiniteSum.Basic` |
| 27 | 638–640 | Suppose that $a_n \geq 0$ for every $n$ and the partial sums $S_n$ are bounded above. Then $\sum_{n=1}^\infty a_n$ converges. | Mathlib | `nonnegative series: bounded partial sums iff summable` in `Mathlib.Topology.Algebra.Order.MonotoneConvergence` |
| 28 | 647–649 | Let $(a_n)$ and $(b_n)$ be non-negative sequences, and suppose that $\exists C, N$ such that $\forall n\geq N$, $a_n \leq Cb_n$. Then if $\sum b_n$ converges, then so does $\sum a_n$. | Mathlib, corrected | `Summable.of_nonneg_of_le / comparison test` in `Mathlib.Topology.Algebra.InfiniteSum.Order` |
| 29 | 715–717 | Let $\sum a_n$ converge absolutely. Then $\sum a_n$ converges. | Mathlib | `Summable.of_norm` in `Mathlib.Analysis.Normed.Group.InfiniteSum` |
| 30 | 735–737 | If $\sum a_n$ converges absolutely, then it converges unconditionally. | Mathlib | `Equiv.summable_iff / summability under reindexing` in `Mathlib.Topology.Algebra.InfiniteSum.Basic` |
| 31 | 756–758 | If $\sum a_n$ converges unconditionally, then it converges absolutely. | Mathlib, corrected | `real unconditional convergence ⇒ absolute convergence / rearrangement theorem family` in `Mathlib.Analysis.SpecificLimits.Normed` |
| 32 | 779–784 | Let $\sum a_n$ be a series that converges absolutely. Then for any bijection $\pi: \N\to \N$, \sum_{n = 1}^\infty a_n = \sum_{n = 1}^\infty a_{\pi(n)}. | Mathlib | `Equiv.tsum_eq` in `Mathlib.Topology.Algebra.InfiniteSum.Basic` |
| 33 | 810–812 | Let $(a_n)$ be a decreasing sequence of non-negative reals, and suppose that $a_n \to 0$. Then $\displaystyle \sum_{n = 1}^\infty (-1)^{n + 1}a_n$ converges, i.e.\ $a_1 - a_2 + a_3 - a_4 + \cdots$ converges. | Mathlib | `alternating-series summability theorem family` in `Mathlib.Analysis.SpecificLimits.Normed` |
| 34 | 838–857 | We have three versions: \item If $\exists c < 1$ such that \frac{\|a_{n + 1}\|}{\|a_n\|} \leq c, for all $n$, then $\sum a_n$ converges. \item If $\exists c < 1$ and $\exists N$ such that (\forall n \geq N)\, \frac{\|a_{n + 1}\|}{\|a_n\|} \leq c, then $\sum a_n$ converges. Note that just because the ratio is always less than $1$, it doesn't necessarily co… | Mathlib, corrected | `ratio-test theorem family` in `Mathlib.Analysis.SpecificLimits.Normed` |
| 35 | 878–883 | Let $(a_n)$ be a decreasing non-negative sequence. Then $\sum_{n = 1}^\infty a_n < \infty$ if and only if \sum_{k = 1}^\infty 2^k a_{2^k} < \infty. | Mathlib, corrected | `Cauchy condensation theorem family` in `Mathlib.Analysis.SpecificLimits.Normed` |
| 36 | 907–909 | Let $f: [1, \infty] \to \R$ be a decreasing non-negative function. Then $\sum_{n = 1}^\infty f(n)$ converges iff $\int_1^\infty f(x)\;\d x < \infty$. | Mathlib, corrected | `integral test / monotone integral-series comparison` in `Mathlib.MeasureTheory.Integral.IntegralEqImproper` |
| 37 | 933–935 | Let $a_1 \geq a_2 \geq \cdots \geq 0$, and suppose that $a_n \to 0$. Let $z\in \C$ such that $\|z\| = 1$ and $z \not= 1$. Then $\sum a_n z^n$ converges. | Mathlib | `Dirichlet/Abel summability theorem family` in `Mathlib.Analysis.SpecificLimits.Normed` |
| 38 | 990–996 | The following two statements are equivalent for a function $f: A\to \R$. \item $f$ is continuous \item If $(a_n)$ is a sequence in $A$ with $a_n \to a$, then $f(a_n) \to f(a)$. | Mathlib | `continuous_iff_seqContinuous` in `Mathlib.Topology.Sequences` |
| 39 | 1037–1044 | Let $A\subseteq \R$ and $f, g: A\to \R$ be continuous functions. Then \item $f + g$ is continuous \item $fg$ is continuous \item if $g$ never vanishes, then $f/g$ is continuous. | Mathlib | `Continuous.add / Continuous.mul / Continuous.div` in `Mathlib.Topology.Algebra.Ring` |
| 40 | 1062–1064 | Let $A, B\subseteq \R$ and $f: A\to B$, $g: B\to \R$. Then if $f$ and $g$ are continuous, $g\circ f: A\to \R$ is continuous. | Mathlib | `Continuous.comp` in `Mathlib.Topology.Basic` |
| 41 | 1077–1079 | Let $[a, b]$ be a closed interval in $\R$ and let $f: [a, b] \to \R$ be continuous. Then $f$ is bounded and attains its bounds, i.e.\ $f(x) = \sup f$ for some $x$, and $f(y) = \inf f$ for some $y$. | Mathlib | `continuous image of compact interval; extrema attained` in `Mathlib.Topology.Instances.Real.Lemmas` |
| 42 | 1091–1093 | Let $a < b\in \R$ and let $f: [a, b] \to \R$ be continuous. Suppose that $f(a) < 0 < f(b)$. Then there exists an $x\in (a, b)$ such that $f(x) = 0$. | Mathlib | `intermediate_value_Icc` in `Mathlib.Topology.Order.IntermediateValue` |
| 43 | 1108–1110 | Let $f: [a, b]\to [c, d]$ be a continuous strictly increasing function with $f(a) = c$, $f(b) = d$. Then $f$ is invertible and its inverse is continuous. | Mathlib | `continuous inverse of a strict monotone interval equivalence` in `Mathlib.Topology.Order.Monotone` |
| 44 | 1122–1130 | Let $a < b$ and let $A\subseteq [a, b]$ have the following properties: \item $a\in A$ \item If $x\in A$ and $x\not= b$, then $\exists y\in A$ with $y > x$. \item If $\forall \varepsilon > 0$, $\exists y\in A: y\in (x - \varepsilon, x]$, then $x\in A$. Then $b\in A$. | Mathlib | `IsClosed.Icc_subset_of_forall_exists_gt` in `Mathlib.Topology.Order.IntermediateValue` |
| 45 | 1139–1147 | Let $A\subseteq [a, b]$ and suppose that \item $a\in A$ \item If $[a, x]\subseteq A$ and $x\not = b$, then there exists $y > x$ such that $[a, y]\subseteq A$. \item If $[a, x)\subseteq A$, then $[a, x]\subseteq A$. Then $A = [a, b]$ | Mathlib | `IsClosed.Icc_subset_of_forall_mem_nhdsWithin` in `Mathlib.Topology.Order.IntermediateValue` |
| 46 | 1160–1162 | Let $a < b\in \R$ and let $f: [a, b] \to \R$ be continuous. Suppose that $f(a) < 0 < f(b)$. Then there exists an $x\in (a, b)$ such that $f(x) = 0$. | Duplicate | `intermediate_value_Icc` in `Mathlib.Topology.Order.IntermediateValue` |
| 47 | 1179–1181 | Let $[a, b]$ be a closed interval in $\R$ and let $f: [a, b] \to \R$ be continuous. Then $f$ is bounded. | Mathlib | `continuous image of compact interval is bounded` in `Mathlib.Topology.Instances.Real.Lemmas` |
| 48 | 1200–1202 | Every cover of a closed, bounded interval $[a, b]$ by open intervals has a finite subcover. We say closed intervals are \emph{compact} (cf.\ Metric and Topological Spaces). | Mathlib | `isCompact_Icc` in `Mathlib.Topology.Instances.Real.Lemmas` |
| 49 | 1220–1222 | Let $[a, b]$ be a closed interval in $\R$ and let $f: [a, b] \to \R$ be continuous. Then $f$ is bounded and attains it bounds, i.e.\ $f(x) = \sup f$ for some $x$, and $f(y) = \inf f$ for some $y$. | Duplicate | `continuous extrema on compact interval` in `Mathlib.Topology.Instances.Real.Lemmas` |
| 50 | 1281–1283 | If $f(x)\to \ell$ and $g(x)\to m$ as $x\to a$, then $f(x) + g(x) \to \ell+ m$, $f(x)g(x) \to \ell m$, and $\frac{f(x)}{g(x)}\to \frac{\ell}{m}$ if $g$ and $m$ don't vanish. | Mathlib, corrected | `Filter.Tendsto.add / mul / div` in `Mathlib.Topology.Algebra.Order` |
| 51 | 1311–1315 | f(x + h) = f(x) + hf'(x) + o(h). | Mathlib, corrected | `HasDerivAt / HasFDerivAt remainder characterisation` in `Mathlib.Analysis.Calculus.Deriv.Basic` |
| 52 | 1323–1325 | If $f(x + h) = f(x) + hf'(x) + o(h)$, then $f$ is differentiable at $x$ with derivative $f'(x)$. | Mathlib, corrected | `hasDerivAt_iff_isLittleO` in `Mathlib.Analysis.Calculus.Deriv.Basic` |
| 53 | 1340–1346 | Let $f, g$ be differentiable at $x$. Then $f + g$ and $fg$ are differentiable at $x$, with (f + g)'(x) &= f'(x) + g'(x)\\ (fg)'(x) &= f'(x)g(x) + f(x)g'(x) | Mathlib | `HasDerivAt.add / HasDerivAt.mul` in `Mathlib.Analysis.Calculus.Deriv.Add` |
| 54 | 1362–1364 | If $f$ is differentiable at $x$ and $g$ is differentiable at $f(x)$, then $g\circ f$ is differentiable at $x$ with derivative $g'(f(x))f'(x)$. | Mathlib | `HasDerivAt.comp` in `Mathlib.Analysis.Calculus.Deriv.Comp` |
| 55 | 1403–1408 | If $f$ and $g$ are differentiable at $x$, and $g(x) \not = 0$, then $f/g$ is differentiable at $x$ with derivative \left(\frac{f}{g}\right)'(x) = \frac{f'(x)g(x) - g'(x)f(x)}{g(x)^2}. | Mathlib | `HasDerivAt.div` in `Mathlib.Analysis.Calculus.Deriv.Mul` |
| 56 | 1418–1420 | If $f$ is differentiable at $x$, then it is continuous at $x$. | Mathlib | `HasDerivAt.continuousAt` in `Mathlib.Analysis.Calculus.Deriv.Basic` |
| 57 | 1426–1430 | Let $f:[a, b]\to [c, d]$ be differentiable on $(a, b)$, continuous on $[a, b]$, and strictly increasing. Suppose that $f'(x)$ never vanishes. Suppose further that $f(a) = c$ and $f(b) = d$. Then $f$ has an inverse $g$ and for each $y\in (c, d)$, $g$ is differentiable at $y$ with derivative $1/f'(g(y))$. In human language, this states that if $f$ is invert… | Mathlib, corrected | `derivative of inverse theorem family` in `Mathlib.Analysis.Calculus.InverseFunction` |
| 58 | 1464–1466 | Let $f$ be continuous on a closed interval $[a, b]$ (with $a < b$) and differentiable on $(a, b)$. Suppose that $f(a) = f(b)$. Then there exists $x\in (a, b)$ such that $f'(x) = 0 $. | Mathlib | `Rolle theorem / exists_deriv_eq_zero` in `Mathlib.Analysis.Calculus.MeanValue` |
| 59 | 1485–1502 | Let $f$ be continuous on $[a, b]$ ($a < b$), and differentiable on $(a, b)$. Then there exists $x\in (a, b)$ such that f'(x) = \frac{f(b) - f(a)}{b - a}. Note that $\frac{f(b) - f(a)}{b - a}$ is the slope of the line joining $f(a)$ and $f(b)$. \draw (0, 0) -- (3, 2); \draw [dashed] (1, 1.26666) -- (2.7, 2.4); \draw (0, 0) .. controls (3, 0) and (0, 2) .. … | Mathlib | `mean value theorem / exists_deriv_eq_slope` in `Mathlib.Analysis.Calculus.MeanValue` |
| 60 | 1535–1541 | Let $f$ be a function with continuous derivative on $(a, b)$. Let $x\in (a, b)$ and suppose that $f'(x) \not= 0$. Then there is an open interval $(u, v)$ containing $x$ on which $f$ is invertible (as a function from $(u, v)$ to $f((u, v))$). Moreover, if $g$ is the inverse, then $g'(f(z)) = \frac{1}{f'(z)}$ for every $z\in (u, v)$. This says that if $f$ h… | Mathlib | `one-dimensional local inverse function theorem` in `Mathlib.Analysis.Calculus.InverseFunction` |
| 61 | 1551–1557 | Let $f$ be continuous on $[a, b]$ ($a < b$) and $n$-times differentiable on an open interval containing $[a, b]$. Suppose that f(a) = f'(a) = f^{(2)}(a) = \cdots = f^{(n - 1)}(a) = f(b) = 0. Then $\exists x\in (a, b)$ such that $f^{(n)}(x) = 0$. | Mathlib, corrected | `higher-order Rolle theorem family` in `Mathlib.Analysis.Calculus.IteratedDeriv` |
| 62 | 1567–1569 | Suppose that $f$ and $g$ are both differentiable on an open interval containing $[a, b]$ and that $f^{(k)}(a) = g^{(k)}(a)$ for $k = 0, 1, \cdots, n - 1$, and also $f(b) = g(b)$. Then there exists $x\in (a, b)$ such that $f^{(n)}(x) = g^{(n)}(x)$. | Mathlib, corrected | `higher-order Rolle applied to f - g` in `Mathlib.Analysis.Calculus.IteratedDeriv` |
| 63 | 1620–1625 | f(a + h) = \underbrace{f(a) + hf'(a) + \cdots + \frac{h^{n - 1}}{(n - 1)!}f^{(n - 1)}(a)}_{(n - 1)\text{-degree approximation to }f\text{ near }a} + \underbrace{\frac{h^n}{n!}f^{(n)}(x)}_{\text{error term}}. for some $x\in (a, a + h)$. | Mathlib, corrected | `Taylor theorem with mean/Lagrange remainder` in `Mathlib.Analysis.Calculus.Deriv.MeanValue` |
| 64 | 1700–1702 | Suppose that $\sum a_nz^n$ converges and $\|w\| < \|z\|$, then $\sum a_n w^n$ converges (absolutely). | Mathlib | `PowerSeries convergence inside a convergence radius` in `Mathlib.Analysis.Calculus.FDeriv.Analytic` |
| 65 | 1753–1759 | The radius of convergence of a power series $\sum a_nz^n$ is R = \frac{1}{\limsup \sqrt[n]{\|a_n\|}}. Often $\sqrt[n]{\|a_n\|}$ converges, so we only have to find the limit. | Mathlib, corrected | `Cauchy–Hadamard/radius formula family` in `Mathlib.Analysis.Calculus.FDeriv.Analytic` |
| 66 | 1794–1796 | The derivative of $e^z$ is $e^z$. | Mathlib | `Complex.hasDerivAt_exp` in `Mathlib.Analysis.SpecialFunctions.ExpDeriv` |
| 67 | 1827–1832 | Let $\sum_{n = 0}^\infty a_n$ and $\sum_{n = 0}^\infty b_n$ be two absolutely convergent series, and let $(c_n)$ be the convolution of the sequences $(a_n)$ and $(b_n)$. Then $\sum_{n = 0}^\infty c_n$ converges (absolutely), and \sum_{n = 0}^{\infty} c_n = \left(\sum_{n = 0}^\infty a_n\right)\left(\sum_{n = 0}^\infty b_n \right). | Mathlib | `Cauchy product / HasSum.mul` in `Mathlib.Topology.Algebra.InfiniteSum.Ring` |
| 68 | 1861–1865 | e^z e^w = e^{z + w}. | Mathlib | `Complex.exp_add` in `Mathlib.Analysis.Complex.Exponential` |
| 69 | 1889–1895 | \frac{\d}{\d z}\sin z &= \frac{ie^{iz} + ie^{-iz}}{2i} = \cos z\\ \frac{\d}{\d z}\cos z &= \frac{ie^{iz} - ie^{-iz}}{2} = -\sin z\\ \sin^2 z + \cos ^2 z &= \frac{e^{2iz} + 2 + e^{-2iz}}{4} + \frac{e^{2iz} - 2 + e^{-2iz}}{-4} = 1. | Mathlib | `Complex.hasDerivAt_sin / hasDerivAt_cos; sin_sq_add_cos_sq` in `Mathlib.Analysis.Complex.Trigonometric` |
| 70 | 1898–1903 | \cos(z + w) &= \cos z\cos w - \sin z\sin w\\ \sin(z + w) &= \sin z \cos w + \cos z \sin w | Mathlib | `Complex.cos_add / Complex.sin_add` in `Mathlib.Analysis.Complex.Trigonometric` |
| 71 | 1939–1948 | \cos \left(z + \frac{\pi}{2}\right) &= -\sin z\\ \sin \left(z + \frac{\pi}{2}\right) &= \cos z\\ \cos (z + \pi) &= -\cos z\\ \sin (z + \pi) &= -\sin z\\ \cos (z + 2\pi) &= \cos z\\ \sin (z + 2\pi) &= \sin z | Mathlib | `π/2 shifts and π-periodicity identities` in `Mathlib.Analysis.Complex.Trigonometric` |
| 72 | 1963–1968 | Let $a$ and $b$ be complex numbers. Then b^n - a^n - n(b - a)a^{n - 1} = (b - a)^2(b^{n - 2} + 2ab^{n - 3} + 3a^2 b^{n - 4} + \cdots + (n - 1)a^{n - 2}). | Mathlib, corrected | `Commute.geom_sum₂_mul plus a finite weighted geometric-sum identity` in `Mathlib.Algebra.Ring.GeomSum` |
| 73 | 1996–1998 | Let $a_n z^n$ have radius of convergence $R$, and let $\|z\| < R$. Then $\sum na_n z^{n - 1}$ converges (absolutely). | Mathlib | `termwise derivative of a power series is summable inside the radius` in `Mathlib.Analysis.Calculus.FDeriv.Analytic` |
| 74 | 2008–2014 | Under the same conditions, \sum_{n = 2}^\infty \binom{n}{2}a_n z^{n -2 } converges absolutely. | Mathlib | `second formal derivative power series is summable` in `Mathlib.Analysis.Calculus.FDeriv.Analytic` |
| 75 | 2020–2026 | Let $\sum a_n z^n$ be a power series with radius of convergence $R$. For $\|z\| < R$, let f(z) = \sum_{n = 0}^\infty a_n z^n\text{ and }g(z) = \sum_{n = 1}^\infty na_nz^{n - 1}. Then $f$ is differentiable with derivative $g$. | Mathlib | `PowerSeries.hasDerivAt / termwise differentiation` in `Mathlib.Analysis.Calculus.FDeriv.Analytic` |
| 76 | 2071–2076 | \frac{\d}{\d z}\cosh z &= \sinh z\\ \frac{\d }{\d z}\sinh z &= \cosh z | Mathlib | `Complex.hasDerivAt_cosh / Complex.hasDerivAt_sinh` in `Mathlib.Analysis.Complex.Trigonometric` |
| 77 | 2078–2083 | \cosh iz &= \cos z\\ \sinh iz &= i\sin z | Mathlib | `Complex.cosh_mul_I / Complex.sinh_mul_I identities` in `Mathlib.Analysis.Complex.Trigonometric` |
| 78 | 2085–2089 | \cosh^2 z - \sinh^2 z = 1, | Mathlib | `Complex.cosh_sq_sub_sinh_sq` in `Mathlib.Analysis.Complex.Trigonometric` |
| 79 | 2136–2141 | If $\mathcal{D}_2$ refines $\mathcal{D}_1$, then U_{\mathcal{D}_2} f \leq U_{\mathcal{D}_1}f\text{ and }L_{\mathcal{D}_2 f}\geq L_{\mathcal{D}_1}f. | Mathlib, corrected | `BoxIntegral.Prepartition order/refinement` in `Mathlib.Analysis.BoxIntegral.Partition.Basic` |
| 80 | 2188–2193 | Let $\mathcal{D}_1$ and $\mathcal{D}_2$ be two dissections of $[a, b]$. Then U_{\mathcal{D}_1}f \geq L_{\mathcal{D}_2}f. | Mathlib, corrected | `common refinement (infimum) and upper/lower comparison` in `Mathlib.Analysis.BoxIntegral.Partition.Basic` |
| 81 | 2274–2281 | This is sometimes known as Cauchy's integrability criterion. Let $f: [a, b] \to \R$. Then $f$ is Riemann integrable if and only if for every $\varepsilon > 0$, there exists a dissection $\mathcal{D}$ such that U_\mathcal{D} - L_\mathcal{D} < \varepsilon. | Mathlib, corrected | `BoxIntegral integrability criterion` in `Mathlib.Analysis.BoxIntegral.Integrability` |
| 82 | 2320–2325 | Let $f: [a, b] \to \R$ be integrable, and $\lambda \geq 0$. Then $\lambda f$ is integrable, and \int_a^b \lambda f(x)\;\d x = \lambda\int_a^b f(x)\;\d x. | Mathlib | `integrability and integral under nonnegative scalar multiplication` in `Mathlib.Analysis.BoxIntegral.Integrability` |
| 83 | 2340–2345 | Let $f: [a, b] \to \R$ be integrable. Then $-f$ is integrable, and \int_a^b -f(x)\;\d x = -\int_a^bf(x)\;\d x. | Mathlib | `integrability and integral under negation` in `Mathlib.Analysis.BoxIntegral.Integrability` |
| 84 | 2368–2373 | Let $f, g: [a, b] \to \R$ be integrable. Then $f + g$ is integrable, and \int_a^b(f(x) + g(x))\;\d x = \int_a^b f(x)\;\d x + \int_a^b g(x)\;\d x. | Mathlib | `integrability and integral under addition` in `Mathlib.Analysis.BoxIntegral.Integrability` |
| 85 | 2397–2402 | Let $f, g: [a, b] \to \R$ be integrable, and suppose that $f(x) \leq g(x)$ for every $x$. Then \int_a^b f(x)\;\d x \leq \int_a^b g(x)\;\d x. | Mathlib | `monotonicity of the integral` in `Mathlib.Analysis.BoxIntegral.Integrability` |
| 86 | 2408–2410 | Let $f: [a, b] \to \R$ be integrable. Then $\|f\|$ is integrable. | Mathlib | `integrability of norm/absolute value` in `Mathlib.Analysis.BoxIntegral.Integrability` |
| 87 | 2444–2450 | Let $f: [a, c] \to \R$ be integrable, and let $b\in (a, c)$. Then the restrictions of $f$ to $[a, b]$ and $[b, c]$ are Riemann integrable, and \int_a^b f(x)\;\d x + \int_b^c f(x)\;\d x = \int_a^c f(x) \;\d x Similarly, if $f$ is integrable on $[a, b]$ and $[b, c]$, then it is integrable on $[a, c]$ and the above equation also holds. | Mathlib | `intervalIntegral.integral_add_adjacent_intervals` in `Mathlib.MeasureTheory.Integral.IntervalIntegral.Basic` |
| 88 | 2486–2488 | Let $f, g: [a, b] \to \R$ be integrable. Then $fg$ is integrable. | Mathlib | `integrability under multiplication` in `Mathlib.Analysis.BoxIntegral.Integrability` |
| 89 | 2509–2511 | Every continuous function $f$ on a closed bounded interval $[a, b]$ is Riemann integrable. | Mathlib | `continuous functions are box/Riemann integrable` in `Mathlib.Analysis.BoxIntegral.Integrability` |
| 90 | 2535–2537 | Let $a < b$ and let $f: [a, b] \to \R$ be continuous. Then $f$ is uniformly continuous. | Mathlib | `IsCompact.uniformContinuousOn_of_continuous` in `Mathlib.Topology.UniformSpace.HeineCantor` |
| 91 | 2552–2554 | Let $f: [a, b] \to \R$ be monotone. Then $f$ is Riemann integrable. | Mathlib, corrected | `monotone functions are Riemann/box integrable` in `Mathlib.Analysis.BoxIntegral.Integrability` |
| 92 | 2595–2597 | Let $a < b$ and let $f$ be a bounded function from $[a, b] \to \R$ that is continuous on $(a, b)$. Then $f$ is integrable. | Mathlib | `bounded, interior-continuous function is integrable` in `Mathlib.Analysis.BoxIntegral.Integrability` |
| 93 | 2631–2633 | Every piecewise continuous and bounded function on $[a, b]$ is integrable. | Mathlib | `piecewise-continuous bounded function is integrable` in `Mathlib.Analysis.BoxIntegral.Integrability` |
| 94 | 2640–2649 | Let $f: [a, b] \to \R$ be Riemann integrable, and for each $n$, let $\mathcal{D}_n$ be the dissection $a = x_0 < x_1 < \cdots < x_n = b$, where $x_i = a + \frac{i(b - a)}{n}$ for each $i$. Then U_{\mathcal{D}_n}f \to \int_a^b f(x)\;\d x and L_{\mathcal{D}_n}f \to \int_a^b f(x)\;\d x. | Mathlib, corrected | `convergence along uniform/unit partitions` in `Mathlib.Analysis.BoxIntegral.UnitPartition` |
| 95 | 2694–2700 | Let $f: [a, b]\to \R$ be continuous, and for $x\in [a, b]$, define F(x) = \int_a^x f(t)\;\d t. Then $F$ is differentiable and $F'(x) = f(x)$ for every $x$. | Mathlib, corrected | `interval-integral fundamental theorem, derivative of indefinite integral` in `Mathlib.MeasureTheory.Integral.IntervalIntegral.FundThmCalculus` |
| 96 | 2717–2722 | If $f$ is continuously differentiable on $[a, b]$, then \int_a^b f'(t)\;\d t = f(b) - f(a). | Mathlib | `intervalIntegral.integral_deriv_eq_sub` in `Mathlib.MeasureTheory.Integral.IntervalIntegral.FundThmCalculus` |
| 97 | 2740–2745 | Let $f: [a, b] \to \R$ be a differentiable function, and suppose that $f'$ is integrable. Then \int_a^b f'(t)\;\d t = f(b) - f(a). | Mathlib | `FTC under integrable derivative hypotheses` in `Mathlib.MeasureTheory.Integral.IntervalIntegral.FundThmCalculus` |
| 98 | 2769–2774 | Let $f, g:[a, b]\to \R$ be integrable such that everything below exists. Then \int_a^b f(x)g'(x)\;\d x = f(b)g(b) - f(a)g(a) - \int_a^b f'(x)g(x)\;\d x. | Mathlib, corrected | `interval integration by parts` in `Mathlib.MeasureTheory.Integral.IntervalIntegral.IntegrationByParts` |
| 99 | 2785–2791 | Let $f$ be $n + 1$ times differentiable on $[a, b]$ with with $f^{(n + 1)}$ continuous. Then f(b) &= f(a) + (b - a)f'(a) + \frac{(b - a)^2}{2!}f^{(2)}(a) + \cdots \\ &+ \frac{(b - a)^n}{n!}f^{(n)}(a) + \int_a^b \frac{(b - t)^n}{n!}f^{(n + 1)}(t)\;\d t. | Mathlib, corrected | `Taylor theorem with integral remainder` in `Mathlib.Analysis.Calculus.TaylorIntegral` |
| 100 | 2821–2826 | Let $f: [a, b] \to \R$ be continuous. Let $g: [u, v] \to \R$ be continuously differentiable, and suppose that $g(u) = a, g(v) = b$, and $f$ is defined everywhere on $g([u, v])$ (and still continuous). Then \int_a^b f(x)\;\d x = \int_u^v f(g(t))g'(t)\;\d t. | Mathlib, corrected | `intervalIntegral.integral_comp_mul_deriv / change of variables` in `Mathlib.MeasureTheory.Integral.IntervalIntegral.IntegrationByParts` |
| 101 | 2880–2882 | Let $f: [1, \infty] \to \R$ be a decreasing non-negative function. Then $\sum_{n = 1}^\infty f(n)$ converges iff $\int_1^\infty f(x)\;\d x < \infty$. | Duplicate | `integral test` in `Mathlib.MeasureTheory.Integral.IntegralEqImproper` |

## Totals

- **Mathlib:** 66
- **Mathlib, corrected:** 31
- **Local proof:** 1
- **Duplicate:** 3

Corrections are detailed in [`SOURCE_CORRECTIONS.md`](SOURCE_CORRECTIONS.md).
